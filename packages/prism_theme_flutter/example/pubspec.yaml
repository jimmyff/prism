name: prism_theme_flutter_example
description: A live demo of prism_theme_flutter — brightness, darkness and morph.
publish_to: none
version: 1.0.0

environment:
  sdk: '>=3.7.0 <4.0.0'
  flutter: '>=3.22.0'

dependencies:
  flutter:
    sdk: flutter
  prism_theme_flutter:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: any

flutter:
  uses-material-design: true
